<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmetsallik.corebanking.transaction.TransactionMapper">

    <insert id="insertTransaction" parameterType="com.hmetsallik.corebanking.transaction.domain.Transaction">
        INSERT INTO transactions (id, account_id, amount, currency, direction, description, balance_after, created_at)
        VALUES (#{id}, #{accountId}, #{amount}, #{currency}, #{direction}, #{description}, #{balanceAfter}, #{createdAt})
    </insert>

    <select id="findTransactionsByAccountId" resultType="com.hmetsallik.corebanking.transaction.domain.Transaction">
        SELECT id, account_id, amount, currency, direction, description, balance_after, created_at
        FROM transactions
        WHERE account_id = #{accountId}
        ORDER BY created_at DESC
    </select>

</mapper>